<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XFproductos"
             x:Class="XFproductos.MainPage">
    <ScrollView>
        <StackLayout Padding="10" Spacing="10">

        <Label Text="Ventas"
               Font="24"
               HorizontalOptions="Center"
               TextColor="Black"/>
        <Entry x:Name="ProductoVendidoEntry"
               Placeholder="Producto Vendido"
               TextColor="Black"
               />
        <Entry x:Name="TotalEntry"
               Placeholder="Cantidad Total"
               Keyboard="Numeric"
               TextColor="Black"/>
        <StackLayout Orientation="Horizontal">
            <Label Text="Fecha"
                  Font="18"
                  VerticalOptions="Center"
                  TextColor="Black"/>
            <DatePicker x:Name="FechaDatePicker"/>


        </StackLayout>

        <Button x:Name="nuevoButton"
                       Text="Nuevo"
                       HorizontalOptions="Center"
                       BackgroundColor="Blue"
                       TextColor="White"/>
        <ListView x:Name="datosListView" />


    </StackLayout>
    </ScrollView>
</ContentPage>
